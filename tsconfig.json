{
  "compilerOptions": {
    "jsx": "react-jsx", 
    "ignoreDeprecations": "5.0",
    "types": ["vite/client"],
    "moduleResolution": "bundler", // <-- AJOUTE CELA pour autoriser les paths sans baseUrl
    "paths": {
      "@/*": ["./src/*"] // <-- Tu peux remettre le "./" sans problème ici
    }
  }
}